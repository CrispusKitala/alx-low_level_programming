#include "main.h"

/**
 * _abs - computing the absolute value of an integer
 * @a: Integer for computing
 *
 * Return: Absolute value
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
